"""Confirmation: promote track display-priority tiers (DESIGN.md §1, Task 3.6).

A RULE on the track (not a framework): persistence + accumulated confidence +
world-location clustering promote a track's tier (contact → candidate → strong). It NEVER
gates visibility — every detection surfaces as at least a faint contact; only the tier
changes. The Localizer (Phase 4) runs on confirmed/promoted contacts.
"""
